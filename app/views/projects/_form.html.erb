<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>
      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  	<%= select("type","id", [["Personal", "Project"],["Collaboration", "Collaboration"], ["Remixable","Remix"], ["Challenge", "Challenge"]], 
	          {:prompt => "Project Type"}, {:class=>"list"}) %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, :cols => 20, :rows => 3 %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
