<%= form_for(@luser) do |f| %>
  <% if @luser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@luser.errors.count, "error") %> prohibited this luser from being saved:</h2>

      <ul>
      <% @luser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
