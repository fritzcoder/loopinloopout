<h1>Listing bank_bookmarks</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bank_bookmarks.each do |bank_bookmark| %>
  <tr>
    <td><%= link_to 'Show', bank_bookmark %></td>
    <td><%= link_to 'Edit', edit_bank_bookmark_path(bank_bookmark) %></td>
    <td><%= link_to 'Destroy', bank_bookmark, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bank bookmark', new_bank_bookmark_path %>
