<%= form_for(@sound_file_bookmark) do |f| %>
  <% if @sound_file_bookmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sound_file_bookmark.errors.count, "error") %> prohibited this sound_file_bookmark from being saved:</h2>

      <ul>
      <% @sound_file_bookmark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
